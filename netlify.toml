[build]
  command = "npm run build"
  publish = ".next"

[functions]
  node_bundler = "esbuild"
  region = "sg" # Runs in Singapore, right next to your MongoDB cluster to make the site load instantly!
